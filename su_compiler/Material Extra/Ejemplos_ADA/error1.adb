procedure Invertir is
    variable : Integer;
   --------------------------------lectura---------------------
   procedure Lectura(X: out Integer; x,y,z : in boolean; m,r,z : in out float) is
   begin
      for I in range.. 12*!$% loop
         Put ("elemento");
         Put (I,0); 
         Put (")= ");
         Get (X(X(Y(a,b,c,)))); 
         Skip_Line;
     end loop;
      New_Line := 12;
   end Lectura;
   ------------------------Invertir-------------------------
   procedure Invertir(X:in out Integer; x,y,z : in Boolean) is
        T:Integer;
		procedure procIn () is 
		begin
			something;
		end proIn;
		variable : Integer;
   begin
      for I in 1..XLast/2 loop
         T:=X(I);
		 x(i, j) := funcion(funcion(funcion()), funcion(x,y,z));
         X(I):=X(N+1-I);
         X(N+1-I):=T;
      end loop;
	  while something not somethingElse loop
		here := something;
	  end loop;
   end Invertir;
   ------------------------f_Invertir-------------------------
   function F_Invertir(X: integer) return Integer is
      T:Integer;
      Y:integer:=X;
	  type arreglo is array (2*3+1..3+2, 2*4*((3)*2)..3) of boolean;
	  
   begin
      for I in 1..inte loop
         T:=Y(I);
		 --type arreglo is array (2*3+1..3+2, 2*4*((3)*2)..3) of boolean;
         Y(I):=Y(N+1-I);
         Y(N+1-I):=T;
      end loop;
      return Y;
    end F_Invertir;
	variable : integer := 12;
   ------------------------------escritura----------------------
   procedure Escritura(X:integer) is
    variable : boolean := asdf;
		procedure otroProc is
		begin
			randomContent;
		end otroProc;
	begin
		for I in variable .. (12+42*12-(34*22)) loop
			Put ("Elemento("); 
			Put (I,0); 
			Put (")= ");
			Put (X(I),0); 
			New_Line;
		end loop;
		variable := 12;
	end Escritura;
   --------------------------principal----------------------
begin
   Put("Teclee longitud del arreglo: ");
   Get(N);
   Skip_Line;
   A := numb;
   Lectura(A);
   Clear_Screen;
   Put ("El Arreglo original es: ");
   if (this < that) then
		something := something;
		get(+23);

	end if;
   New_Line;
   Escritura(A);
   Invertir(A);
   Put ("Arreglo invertido es: ");
   New_Line;
   Escritua(A);
   Put ("Arreglo nuevamente invertido es: ");
   New_Line;
   Escritura(F_Invertir(A));
   New_Line;
   Put ("Teclee <<enter>> para continuar");
  get(hola);
   Skip_Line;
end Invertir;
