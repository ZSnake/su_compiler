procedure f1 is
       
   -- A small function.
   function Sumsqr(X, Y: Integer) return integer is
   begin
      return X*X + Y*Y;
   end sumsqqr;
   
    procedure procedimientoAnidado (variable : in integer; v,a,r : out boolean; i,a,b,l,e : in out float) is
    type arregloUni is array ((2+3*3+2+variable)..4) of integer; --Definiendo un arreglo
    begin
         contenido := something;
    end procedimientoAnidado;
    --Declaracion de otras variables para el proc original
     var1, var2, var3 : Integer := 12;

   -- How 'bout a nice, tender variable?
    x, y, z : integer := 0;
   I: Integer;
--begin procedure
begin
   I := Sumsqr( 3+ funcion(var2), 14*15+ var1);
   var3:= funcion(funcion(funcion()));
   Put(I);
   New_Line;
   put(var1, var2, var3);
------------------------------------------------------------------------------------> Tryin some if stuff
  if (something and (something or something) and this) then
       var := somethingElse;
   elsif (something or (something and something)) then
        var := anotherSomething;
   elsif (something and something) then
        put("Something to put in here");
    else
        get(variable);
        put ("putting something");
    end if;
--------------------------------------------------------------------------------------> Trying some loops (for, while, loop)
loop 
     something := something;
      for i in variable .. metodo(variable, numero, true) loop
               var1:= algo;
               while something and (something or something) loop
                       asigno_matriz(12,llamadoAFuncion(pa,ra,me,tros)) := (1 + ( 13 +( 25 * (12+3*4)-5)*12)+13 ) *2;   --no hae nada
               end loop;
     end loop;
end loop;
   Put(Sumsqr(I, 4));
   New_Line;
end f1;





